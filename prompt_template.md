https://chat.deepseek.com/share/mxe1zda2rdq9q0t2cs


**Критерии качества:**
1. **Код должен быть рабочим:** Синтаксически корректный YAML, правильные имена модулей (не устаревшие).
2. **Идемпотентность:** Задачи используют модули, которые не создают дублирующих записей (например, `state: present`, `creates:`).
3. **Следует best practices:** Используй `notify` для handlers, `become: yes` для привилегированных задач, явное указание `when` для условного выполнения.
4. **Документированность:** В `meta/main.yml` укажи автор, supported platforms. В `README.md` (если запрошу) — краткое описание, переменные, пример использования.
5. **Конкретность:** Не используй плейсхолдеры вроде `...` или `# остальное`. Генерируй полный, законченный код.

**Переменная часть (описание сервиса):**

{СЮДА ВСТАВЛЯЮ ОПИСАНИЕ СЕРВИСА: НАЗВАНИЕ, ВЕРСИЯ, ПОРТ, КОНФИГ, ЗАВИСИМОСТИ, ОСОБЫЕ ТРЕБОВАНИЯ}

---

### Пример использования шаблона (как я буду подставлять данные)

**Входные данные (переменная часть):**

Сервис: Nginx
Версия: 1.24
Порт: 8080 (заменить стандартный 80)
Конфиг: добавить поддержку виртуальных хостов, конфиги хранить в /etc/nginx/sites-available/
Особенности: установка только на Ubuntu 22.04, нужно добавить handler для перезагрузки при изменении конфига


**Промпт целиком** = шаблон выше + этот блок с описанием.

---

### Документ для хранения шаблона

Поскольку вы просили предоставить ссылку на документ, я оформил бы его в Яндекс Документе (или текстовом файле).

**Ссылка на решение (пример для демонстрации):**  
*В реальном выполнении вы создаёте файл и даёте ссылку. Так как это учебный пример, я приведу структуру документа.*

**Содержимое документа (`ansible_role_template.md`):**

```markdown
# Шаблон промпта: Ansible роль

## Описание задачи
DevOps задача: регулярное создание Ansible ролей для инфраструктурных сервисов. Требуется структурированный, идемпотентный код с тестами и документацией.

## Шаблон промпта (копировать для каждого использования)

Роль: Ты — Senior DevOps инженер с 8-летним опытом, эксперт по Ansible. Ты пишешь чистый, модульный, идемпотентный код, следуешь Ansible Best Practices. Ты никогда не оставляешь секреты в коде.

Контекст: Мне нужно быстро создавать новые Ansible роли для production. Роль должна иметь правильную структуру, переменные по умолчанию, handlers, поддержку разных ОС, тесты molecule.

Задача: Создай Ansible роль для установки и настройки сервиса, описанного ниже. Требования:
- структура tasks, handlers, defaults, vars, templates, meta, molecule
- идемпотентность
- переменные в defaults
- handler на перезапуск
- поддержка RedHat/Debian через os_family
- безопасность (нет хардкода секретов)
- базовый тест molecule

Формат: Markdown. Для каждого файла — заголовок с путём и блок кода с языком.

Критерии:
- корректный синтаксис YAML
- использование современных модулей
- notify для handlers
- meta с автором и платформами
- без плейсхолдеров, полный код

Переменная часть (описание сервиса):

{СЮДА ВСТАВЛЯЮ: название, версия, порт, конфиги, ОС, особые требования}
